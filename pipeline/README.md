# Groundtruth Pipeline

Python package for deterministic geospatial ingestion and validation.

Phase 0 only provides the package skeleton and `gt` CLI entrypoint. Later
phases add migrations, region scaffolding, layer runners, validation, QA, and
promotion commands.

